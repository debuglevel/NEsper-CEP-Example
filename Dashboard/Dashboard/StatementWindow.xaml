<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:CEP.Dashboard" x:Class="CEP.Dashboard.StatementWindow"
        Title="StatementWindow" Height="471" Width="697">
    <Window.DataContext>
        <local:Data/>
    </Window.DataContext>
    <Grid>

        <Button x:Name="ButtonGetStatements" Content="Get Statements" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Click="ButtonGetStatements_Click" />
        <ListView x:Name="StatementsListView" ItemsSource="{Binding Statements}" Margin="10,37,10,0" Height="147" VerticalAlignment="Top">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name"
						DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="CQL"
						DisplayMemberBinding="{Binding CQL}" />
                </GridView>
            </ListView.View>
        </ListView>
        <TextBox HorizontalAlignment="Left" Margin="10,189,0,10" TextWrapping="Wrap" Text="{Binding Path=CQL}" Width="661" FontFamily="DejaVu Sans Mono" FontSize="16" DataContext="{Binding SelectedItem, ElementName=StatementsListView, Mode=TwoWay}" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Visible"/>

    </Grid>
</Window>
